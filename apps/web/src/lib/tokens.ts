/**
 * Design tokens for JS consumers (theme-color meta, Razorpay theme, ...).
 * The CSS custom properties come from `@katha/tokens/tokens.css`, generated from the same source.
 */
export { colors, radii, spacing, type as typeTokens } from "@katha/tokens";
